# EDOCS / ECMDB 生态系统全局架构约束与 AI 准则 (AGENTS.md)

## ⚠️ 架构认知铁律（禁止违背）

### 1. 独立微服务矩阵，绝非单向流水线
- **各子系统完全独立自治**：
  - **ECMDB**：配置项元数据驱动与资产关系拓扑中枢。
  - **EFlow**：低代码工作流引擎与工单流转系统。
  - **ETask**：分布式异步运维作业执行器。
  - **EIAM**：多租户统一身份凭证与 PBAC 策略鉴权中心。
  - **EAlert**：监控告警接入、降噪与排班调度引擎。
- **严禁使用单向流水线描述**：
  - ❌ **严禁表述为**：`身份 -> 资产 -> 工单 -> 任务 -> 告警` 等任何带有前后递进、顺序箭头（`->`）的流水线或瀑布式流转闭环。
  - ✅ **必须表述为**：各子系统松耦合独立部署、独立演进、按需插拔；协同通过标准 RPC 契约与异步事件总线进行**按需微服务网状调用**。
  - 各系统既可以单独运行（如单独部署 ECMDB 做资产管理，或单独部署 EIAM 做统一鉴权），也可以在统一控制台（ECMDB-Web）下聚合联动。

### 2. 微服务间交互模型与现状边界
- **当前现状边界（重要）**：
  - ⚠️ **EFlow 目前尚未与 ECMDB 组合打通**。EFlow 专注于独立的表单构建与审批流程引擎，切勿过度脑补工单修改 CMDB 资产或流程回写资产状态。
- **调用方式**：通过 Protobuf / gRPC 同步调用、RESTful API 契约或 Kafka 异步事件总线。
- **微服务协同示例**：
  - EFlow 工单审批通过后，可按需调用 ETask 异步下发执行脚本。
  - EAlert 触发告警后，可直接匹配排班并推送通知。
  - 所有微服务在接受外部请求时，均通过 EIAM 进行统一身份与权限策略校验。
  - 各服务之间是**网状协作与按需调用**，不存在“谁必须作为谁的下游”的前置条件。
